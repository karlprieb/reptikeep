diff --git a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModifierRegistry.kt b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModifierRegistry.kt
index d442c5e..674b66a 100644
--- a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModifierRegistry.kt
+++ b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModifierRegistry.kt
@@ -63,7 +63,9 @@ import androidx.compose.ui.layout.onVisibilityChanged
 import androidx.compose.ui.layout.positionInWindow
 import androidx.compose.ui.platform.LocalDensity
 import androidx.compose.ui.semantics.Role
+import androidx.compose.ui.semantics.contentDescription
 import androidx.compose.ui.semantics.contentType
+import androidx.compose.ui.semantics.role
 import androidx.compose.ui.semantics.semantics
 import androidx.compose.ui.unit.DpOffset
 import androidx.compose.ui.unit.dp
@@ -657,9 +659,19 @@ object ModifierRegistry {
 
     register("semantics") { map, _, _, _ ->
       val params = recordFromMap<SemanticsParams>(map)
-      params.contentType.toContentType()?.let { ct ->
-        Modifier.semantics { contentType = ct }
-      } ?: Modifier
+      val resolvedContentType = params.contentType.toContentType()
+      val description = params.contentDescription
+      val resolvedRole = params.role.toSemanticsRole()
+
+      if (resolvedContentType == null && description == null && resolvedRole == null && !params.mergeDescendants) {
+        Modifier
+      } else {
+        Modifier.semantics(mergeDescendants = params.mergeDescendants) {
+          resolvedContentType?.let { contentType = it }
+          description?.let { contentDescription = it }
+          resolvedRole?.let { role = it }
+        }
+      }
     }
 
     register("clip") { map, _, _, _ ->
diff --git a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/RNHostView.kt b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/RNHostView.kt
index 29e36b6..ff13ed3 100644
--- a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/RNHostView.kt
+++ b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/RNHostView.kt
@@ -12,6 +12,7 @@ import androidx.compose.foundation.layout.fillMaxSize
 import androidx.compose.foundation.layout.requiredSize
 import androidx.compose.runtime.Composable
 import androidx.compose.runtime.MutableState
+import androidx.compose.runtime.key
 import androidx.compose.runtime.mutableStateOf
 import androidx.compose.ui.Modifier
 import androidx.compose.ui.layout.onGloballyPositioned
@@ -43,7 +44,7 @@ internal data class RNHostViewProps(
   val matchContents: MutableState<Boolean?> = mutableStateOf(null),
   //  Adds LeafNode and MeasurableYogaNode trait in Shadow node
   val expoInternalSizeFromChildren: MutableState<Boolean?> = mutableStateOf(null),
-  val modifiers: ModifierList = emptyList()
+  val modifiers: MutableState<ModifierList> = mutableStateOf(emptyList())
 ) : ComposeProps
 
 @SuppressLint("ViewConstructor")
@@ -159,16 +160,24 @@ internal class RNHostView(context: Context, appContext: AppContext) :
       // Origin last: a chain applies outside-in, so a caller `padding` or `offset` has to shift the
       // content before it is read.
       val modifiers = sizingModifier
-        .then(ModifierRegistry.applyModifiers(props.modifiers, appContext, scope, globalEventDispatcher))
+        .then(ModifierRegistry.applyModifiers(props.modifiers.value, appContext, scope, globalEventDispatcher))
         .then(publishContentOriginModifier())
 
-      AndroidView(
-        factory = {
-          detachForReuse(wrapper)
-          wrapper
-        },
-        modifier = modifiers
-      )
+      // `AndroidView`'s `factory` runs once per composition slot and is never re-invoked just
+      // because `wrapper` (the captured variable) points at a different instance on a later
+      // recomposition — there is no `update` callback here to hand it the new instance. Without
+      // this `key`, a `wrapperState` change (this view reused for a different child, e.g. after
+      // Fabric hands this RNHostView instance back out for an unrelated `<Host>`) leaves the old
+      // child's view on screen forever instead of swapping to the new one.
+      key(wrapper) {
+        AndroidView(
+          factory = {
+            detachForReuse(wrapper)
+            wrapper
+          },
+          modifier = modifiers
+        )
+      }
     }
   }
 
diff --git a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/SemanticsModifier.kt b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/SemanticsModifier.kt
index fd8cfe5..52e8d27 100644
--- a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/SemanticsModifier.kt
+++ b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/SemanticsModifier.kt
@@ -1,15 +1,30 @@
 package expo.modules.ui
 
 import androidx.compose.ui.autofill.ContentType
+import androidx.compose.ui.semantics.Role
 import expo.modules.kotlin.records.Field
 import expo.modules.kotlin.records.Record
 import expo.modules.kotlin.types.OptimizedRecord
 
 @OptimizedRecord
 internal data class SemanticsParams(
-  @Field val contentType: String? = null
+  @Field val contentType: String? = null,
+  @Field val contentDescription: String? = null,
+  @Field val role: String? = null,
+  @Field val mergeDescendants: Boolean = false
 ) : Record
 
+internal fun String?.toSemanticsRole(): Role? = when (this) {
+  "button" -> Role.Button
+  "checkbox" -> Role.Checkbox
+  "switch" -> Role.Switch
+  "radioButton" -> Role.RadioButton
+  "tab" -> Role.Tab
+  "image" -> Role.Image
+  "dropdownList" -> Role.DropdownList
+  else -> null
+}
+
 internal fun String?.toContentType(): ContentType? = when (this) {
   "email" -> ContentType.EmailAddress
   "username" -> ContentType.Username
diff --git a/node_modules/@expo/ui/build/jetpack-compose/modifiers/index.d.ts b/node_modules/@expo/ui/build/jetpack-compose/modifiers/index.d.ts
index e09bd93..9f0dc0c 100644
--- a/node_modules/@expo/ui/build/jetpack-compose/modifiers/index.d.ts
+++ b/node_modules/@expo/ui/build/jetpack-compose/modifiers/index.d.ts
@@ -307,6 +307,9 @@ export declare const testID: (tag: string) => import("./createModifier").Modifie
  */
 export declare const semantics: (params: {
     contentType?: string;
+    contentDescription?: string;
+    role?: 'button' | 'checkbox' | 'switch' | 'radioButton' | 'tab' | 'image' | 'dropdownList';
+    mergeDescendants?: boolean;
 }) => import("./createModifier").ModifierConfig;
 type MaterialShapeName = 'cookie4Sided' | 'cookie6Sided' | 'cookie7Sided' | 'cookie9Sided' | 'cookie12Sided' | 'clover4Leaf' | 'clover8Leaf' | 'softBurst' | 'boom' | 'oval' | 'pill' | 'triangle' | 'diamond' | 'pentagon' | 'sunny' | 'verySunny' | 'fan' | 'pixelCircle' | 'pixelTriangle' | 'ghostish' | 'bun' | 'heart' | 'arch' | 'slanted' | 'puffy' | 'puffyDiamond';
 type CornerRadii = {
diff --git a/node_modules/@expo/ui/ios/ChartView.swift b/node_modules/@expo/ui/ios/ChartView.swift
index 664eb10..44b7fb9 100644
--- a/node_modules/@expo/ui/ios/ChartView.swift
+++ b/node_modules/@expo/ui/ios/ChartView.swift
@@ -137,15 +137,13 @@ public struct ChartView: ExpoSwiftUI.View {
         .foregroundStyle(props.lineStyle?.color ?? .blue)
         .symbol(getSymbol(props.lineStyle?.pointStyle ?? .circle))
         .symbolSize(CGFloat(props.lineStyle?.pointSize ?? 6.0))
-        .lineStyle(getLineStyle(props.lineStyle?.dashArray ?? []))
-        .lineStyle(.init(lineWidth: CGFloat(props.lineStyle?.width ?? 2.0)))
+        .lineStyle(getLineStyle(props.lineStyle?.dashArray ?? [], props.lineStyle?.width ?? 2.0))
     } else {
       LineMark(x: .value("X", dataPoint.xValue), y: .value("Y", dataPoint.y))
         .foregroundStyle(props.lineStyle?.color ?? .blue)
         .symbol(getSymbol(props.lineStyle?.pointStyle ?? .circle))
         .symbolSize(CGFloat(props.lineStyle?.pointSize ?? 6.0))
-        .lineStyle(getLineStyle(props.lineStyle?.dashArray ?? []))
-        .lineStyle(.init(lineWidth: CGFloat(props.lineStyle?.width ?? 2.0)))
+        .lineStyle(getLineStyle(props.lineStyle?.dashArray ?? [], props.lineStyle?.width ?? 2.0))
     }
   }
 
@@ -254,7 +252,7 @@ public struct ChartView: ExpoSwiftUI.View {
     }
   }
 
-  private func getLineStyle(_ dashArray: [Double]) -> StrokeStyle {
-    StrokeStyle(dash: dashArray.map { CGFloat($0) })
+  private func getLineStyle(_ dashArray: [Double], _ width: Double) -> StrokeStyle {
+    StrokeStyle(lineWidth: CGFloat(width), dash: dashArray.map { CGFloat($0) })
   }
 }
diff --git a/node_modules/@expo/ui/ios/ImageView.swift b/node_modules/@expo/ui/ios/ImageView.swift
index 170de73..1e67acf 100644
--- a/node_modules/@expo/ui/ios/ImageView.swift
+++ b/node_modules/@expo/ui/ios/ImageView.swift
@@ -12,6 +12,37 @@ public final class ImageViewProps: UIBaseViewProps {
   var onTap = EventDispatcher()
 }
 
+private enum DecodedImageCache {
+  static let shared: NSCache<NSString, UIImage> = {
+    let cache = NSCache<NSString, UIImage>()
+    cache.totalCostLimit = 64 * 1024 * 1024
+    return cache
+  }()
+
+  static func image(for source: String?) -> UIImage? {
+    guard let source, let url = URL(string: source) else {
+      return nil
+    }
+
+    let attributes = try? FileManager.default.attributesOfItem(atPath: url.path)
+    let size = (attributes?[.size] as? NSNumber)?.int64Value ?? 0
+    let modified = (attributes?[.modificationDate] as? Date)?.timeIntervalSinceReferenceDate ?? 0
+    let key = "\(url.absoluteString)@\(size):\(String(format: "%.6f", modified))" as NSString
+
+    if let cached = shared.object(forKey: key) {
+      return cached
+    }
+
+    guard let data = try? Data(contentsOf: url), let image = UIImage(data: data) else {
+      return nil
+    }
+
+    let pixels = image.size.width * image.scale * image.size.height * image.scale
+    shared.setObject(image, forKey: key, cost: Int(pixels) * 4)
+    return image
+  }
+}
+
 public struct ImageView: ExpoSwiftUI.View {
   @ObservedObject public var props: ImageViewProps
 
@@ -41,10 +72,7 @@ public struct ImageView: ExpoSwiftUI.View {
       symbolImage
         .applyImageModifiers(props.modifiers, appContext: props.appContext)
         .applyOnTapGesture(useTapGesture: props.useTapGesture, eventDispatcher: props.onTap)
-    } else if let url = props.uiImage,
-              let url = URL(string: url),
-              let data = try? Data(contentsOf: url),
-              let uiImage = UIImage(data: data) {
+    } else if let uiImage = DecodedImageCache.image(for: props.uiImage) {
       Image(uiImage: uiImage)
         .applyImageModifiers(props.modifiers, appContext: props.appContext)
         .applyOnTapGesture(useTapGesture: props.useTapGesture, eventDispatcher: props.onTap)
diff --git a/node_modules/@expo/ui/src/jetpack-compose/modifiers/index.ts b/node_modules/@expo/ui/src/jetpack-compose/modifiers/index.ts
index 398f80a..1884869 100644
--- a/node_modules/@expo/ui/src/jetpack-compose/modifiers/index.ts
+++ b/node_modules/@expo/ui/src/jetpack-compose/modifiers/index.ts
@@ -445,8 +445,24 @@ export const testID = (tag: string) => createModifier('testID', { testID: tag })
 
 /**
  * Applies semantic properties. Wraps `Modifier.semantics { ... }`.
- */
-export const semantics = (params: { contentType?: string }) => createModifier('semantics', params);
+ * @param params.contentType - Autofill content type hint.
+ * @param params.contentDescription - Accessible name announced by TalkBack.
+ * @param params.role - Semantic role: 'button', 'checkbox', 'switch', 'radioButton', 'tab', 'image', or 'dropdownList'.
+ * @param params.mergeDescendants - Merge child semantics into this node so it is announced as one element.
+ */
+export const semantics = (params: {
+  contentType?: string;
+  contentDescription?: string;
+  role?:
+    | 'button'
+    | 'checkbox'
+    | 'switch'
+    | 'radioButton'
+    | 'tab'
+    | 'image'
+    | 'dropdownList';
+  mergeDescendants?: boolean;
+}) => createModifier('semantics', params);
 
 // =============================================================================
 // Clip Modifier & Shapes
